1.  select count(empno) as employees from emp;
2.  select count(dname) as departments from dept;
3.  select ename from emp;
4.  select dname from dept;
5.  select sum(sal) as total_sal from emp;
6.  select sum(comm) as total_comm from emp;
7.  select job from emp where comm is not null;
8.  select sysdate from dual;
9.  select avg(sal) as avg_sal from emp;
10. select deptno, count(empno) as employees from emp group by deptno order by deptno;
11. select deptno, sum(sal) as total_sal from emp group by deptno order by deptno;
12. select job, count(empno) as employees from emp group by job;
13. select job, avg(sal) as avg_sal from emp group by job;
14. select ename, to_char(hiredate,'DD-MM-YYYY') as hiredate from emp;
15. select ename, deptno from emp order by deptno;
16. select ename, job from emp order by job;
17. select ename, sal from emp order by sal desc;
18. select ename, deptno, sal from emp order by deptno asc, sal desc;
19. select count(ename) from emp where length(ename)=6;
20. select min(sal), max(sal) from emp;
21. select deptno, max(sal), min(sal), avg(sal), sum(sal) from emp group by deptno order by deptno;
22. select ename, hiredate from emp order by hiredate;
23. select ename, hiredate as latest+emp from emp order by hiredate desc fetch first 1 rows only;
24. select ename, hiredate as oldest_emp from emp order by hiredate asc fetch first 1 rows only;
25. select ename, to_char(hiredate,'YYYY') as year, deptno from emp order by year desc,deptno asc;
26. select ename from emp where sal>=(select avg(sal) from emp);
27. select ename from emp where sal<=(select avg(sal) from emp);
28. select ename from emp where sal between 2000 and 4000;
29. select ename, sal as max_sal from emp where sal = (select max(sal) from emp);
    select ename, sal as min_sal from emp where sal = (select min(sal) from emp);
30. select count(empno) as gifts from emp where extract(month from hiredate)=extract(month from add_months(sysdate,1));